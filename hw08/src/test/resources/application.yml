spring:
  data:
    mongodb:
      host: localhost
      database: testdb
      port: 0
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2
mongock:
  enabled: false
  mongo-db:
    write-concern:
      journal: false
    read-concern: local