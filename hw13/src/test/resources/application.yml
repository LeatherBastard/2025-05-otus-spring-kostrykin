spring:
  flyway:
    enabled: true
  datasource:
    url: jdbc:h2:mem:testdb;MODE=LEGACY
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: never


