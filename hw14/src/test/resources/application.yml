spring:
  shell:
    interactive:
      enabled: false
    script:
      enabled: false
    command:
      version:
        enabled: false
  batch:
    job:
      enabled: false

  data:
    mongodb:
      uri: mongodb://admin:password@localhost:27017/library?authSource=admin
      host: localhost
      port: 27017
      database: library
      authentication-database: admin

  logging:
    level:
      de:
        flapdoodle:
          embed: DEBUG
        org:
          springframework:
            data:
              mongodb: DEBUG