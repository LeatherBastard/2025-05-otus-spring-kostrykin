spring:
  shell:
    interactive:
      enabled: false
    script:
      enabled: false
    command:
      version:
        enabled: false
  batch:
    job:
      enabled: false

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 6.0.5

  data:
    mongodb:
      host: localhost
      database: library
      port: 0
