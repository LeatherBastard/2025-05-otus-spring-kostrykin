spring:
  shell:
    interactive:
      enabled: true
    script:
      enabled: true
  output:
    ansi:
      enabled:ALWAYS
  messages:
    baseName: messages
    encoding: UTF-8
    fallbackToSystemLocale: false

test:
  rightAnswersCountToPass: 3
  locale: ru-Ru
  fileNameByLocaleTag:
    ru-RU: questions_ru.csv
    en-US: questions.csv